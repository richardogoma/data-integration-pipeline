    1  git status
    2  git fetch origin
    3  git status
    4  git pull origin main
    5  cls
    6  clear
    7  docker-compose up sql-server-db -d
    8  clear
    9  docker-compose up -d sql-server-db
   10  clear
   11  docker-compose ps
   12  docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' dc_sql-server-db
   13  clear
   14  docker-compose up --build --remove-orphans -d
   15  clear
   16  docker-compose up --build --remove-orphans -d
   17  clear
   18  docker exec -it dc_sql-server-db /opt/mssql-tools/bin/sqlcmd -S 172.18.0.2,1433 -U sa -P P@s5wOrd
   19  clear
   20  docker-compose ps -a
   21  docker-compose ls -a
   22  docker-compose down
   23  docker-compose ps -a
   24  docker container ls -a
   25  docker rmi ls -a
   26  docker rmi --help
   27  docker image ls -a
   28  docker image prune
   29  docker image ls -a
   30  clear
   31  docker-compose up --build --remove-orphans -d
   32  clear
   33  docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' dc_sql-server-db
   34  docker-compose ps -a
   35  docker-compose up --build --remove-orphans -d
   36  clear
   37  docker-compose up --build --remove-orphans -d
   38  CLS
   39  cls
   40  clear
   41  echo $USER
   42  clear
   43  docker-compose up --build --remove-orphans -d
   44  clear
   45  docker-compose ps -a
   46  clear
   47  docker-compose down
   48  docker container ls -a
   49  docker image ls -a
   50  docker image prune
   51  clear
   52  docker image ls -a
   53  clear
   54  docker-compose up --build --remove-orphans -d
   55  echo $DB_USER
   56  clear
   57  docker-compose down
   58  docker container ls -a
   59  docker image ls -a
   60  docker image prune
   61  clear
   62  docker-compose up -d sql-server-db
   63  docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' dc_sql-server-db
   64  docker-compose up --build data-integrator --remove-orphans -d
   65  docker-compose up data-integrator --build --remove-orphans -d
   66  docker-compose up --build --remove-orphans -d
   67  echo $User
   68  echo $DB_USER
   69  clear
   70  docker-compose up --build --remove-orphans -d
   71  clear
   72  history
   73  clear
   74  docker container ls -a
   75  docker inspect -f '{{range $p, $conf := .NetworkSettings.Ports}}{{(index $conf 0).HostPort}} -> {{$p}}{{end}}' dc_sql-server-db
   76  clear
   77  docker-compose down
   78  docker container ps -a
   79  docker image ps -a
   80  docker image ls -a
   81  docker image prune
   82  clear
   83  history > history.txt
